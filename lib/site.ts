/**
 * Central link config. Replace the TODO values before launch.
 */
export const LINKS = {
  /** In-page anchor to the final CTA section */
  leakAudit: "#leak-audit",
  /** TODO: replace with the real audit request form URL when it exists */
  auditRequest: "mailto:hello@awaazlabs.io?subject=Free%20leak%20audit%20request",
  /** TODO: replace with the live Layla demo line */
  demoCall: "tel:+0000000000",
} as const;

export const TAGLINE = "Miss nothing. Book everything.";
